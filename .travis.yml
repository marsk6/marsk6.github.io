language: node_js
node_js:
- lts/*
branches:
  only:
    - master
cache:
  directories:
    - node_modules

script:
- yarn build

deploy:
  provider: pages
  cleanup: false
  token: $GITHUB_TOKEN
  keep_history: true
  on:
    branch: master
  local_dir: dist